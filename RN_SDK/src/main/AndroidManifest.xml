<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rnsdk">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-feature android:name="android.hardware.camera"></uses-feature>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application   android:requestLegacyExternalStorage="true">
        <activity android:name=".Activities.WebViewActivity"></activity>
        <activity android:name=".Activities.TakeSurveyActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activities.LocationActivity"
            android:label="@string/title_activity_location" />
        <activity android:name=".Activities.NotificationDetailActivity" />
        <activity android:name=".Activities.NotificationActivity" />
        <activity android:name=".Activities.TransactionHistoryActivity" />
        <activity android:name=".Activities.ProfileActivity" />
        <activity android:name=".Activities.ContactUsActivity" />
        <activity android:name=".Activities.LeaderboardActivity" />
        <activity android:name=".Activities.ChangePasswordActivity" />
        <activity android:name=".Activities.UploadReceiptActivity" />
        <activity android:name=".Activities.TransferPointActivity" />
        <activity android:name=".Activities.WaysToEarnActivity" />
        <activity android:name=".Activities.OffersDetailActivity" />
        <activity android:name=".Activities.OfferActivity" />
        <activity android:name=".Activities.ReferFriendActivity" />
        <activity android:name=".Activities.RewardEntryGoalActivity" />
        <activity android:name=".Activities.CashbackActivity" />
        <activity android:name=".Activities.CustomDrawer" />
        <activity android:name=".Activities.HomeActivity" />
        <activity android:name=".Activities.LoginActivities.VideoMotionLoginActivity" />
        <activity android:name=".Activities.LoginActivities.CleanLoginActivity" />
        <activity android:name=".Activities.LoginActivities.CleanButtonsLoginActivity" />
        <activity android:name=".Activities.SplashActivity" />
    </application>

</manifest>